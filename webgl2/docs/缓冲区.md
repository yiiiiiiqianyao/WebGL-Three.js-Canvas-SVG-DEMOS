var buffer = gl.createBuffer(); // 创建缓冲区
gl.bindBuffer(gl.ARRAY_BUFFER, buffer); // 将 buffer 标识符表示的缓冲区对象指定为当前缓冲区
---- gl.ARRAY_BUFFER 表示 buffer 缓冲区的中的数据是顶点数据

当有一个缓冲区对象被指定为当前缓冲区时，后面所有将数据放入缓冲区的函数都使用当前缓冲区，直到绑定另一个缓冲
区对象为当前缓冲区